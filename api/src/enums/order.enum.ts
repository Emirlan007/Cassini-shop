export enum DeliveryStatus {
  Warehouse = 'warehouse',
  OnTheWay = 'on_the_way',
  Delivered = 'delivered',
}
export enum OrderStatus {
  AwaitingPayment = 'awaiting_payment',
  Paid = 'paid',
  Canceled = 'canceled',
}
